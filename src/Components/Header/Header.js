import React from 'react'


import 'Header.css'
import Timer from 'Timer'

const Header = (props) => {

    return (
        <header>
            <h1>ToDo List</h1>
                <Timer/>
                <p>Looks like another Wednesday</p>
            </div>
        </header>
    )
}